#include <iostream>
#include <algorithm>
using namespace std;

const int inf = 5010;
int dp[inf][inf];

int main() {
    freopen("作业/data/a.in", "r", stdin);
    freopen("作业/data/a.out", "w", stdout);
    int n, a, b, k;
    cin >> n >> a >> b >> k;

    

    return 0;
}